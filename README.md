# ofkm-shadcn-svelte-registry

### Install Guide

Replace <component> with the specific component from the registry.

```bash
pnpm dlx shadcn-svelte@latest add https://shadcn.ofkm.dev/r/<component>.json
```
